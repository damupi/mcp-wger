# Public wger.de instance (default — no need to set if using wger.de)
# WGER_BASE_URL=https://wger.de/api/v2

# Self-hosted instance (uncomment and set your URL)
# WGER_BASE_URL=http://localhost:80/api/v2

# API token — get it from wger at Settings → API key
WGER_API_TOKEN=your-token-here
